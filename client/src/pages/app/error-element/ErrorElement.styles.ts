import styled from 'styled-components';

const Error = styled.main`
  font-size: var(--fs-l);
  display: grid;
  place-content: center;
  text-align: center;
  min-height: 100vh;
`;

export { Error };
