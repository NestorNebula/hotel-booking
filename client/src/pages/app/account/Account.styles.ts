import styled from 'styled-components';

const Account = styled.main``;
const Infos = styled.section``;
const CloseButton = styled.button``;
const Reservations = styled.section``;

export { Account, Infos, CloseButton, Reservations };
